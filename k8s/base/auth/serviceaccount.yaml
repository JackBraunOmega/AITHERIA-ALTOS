apiVersion: v1
kind: ServiceAccount
metadata:
  name: auth-service-sa
  labels:
    service: auth
    tier: backend
  annotations:
    # These annotations would typically be added in environment-specific overlays
    # eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/auth-service-role
    description: "Service account for Auth microservice in AITHERIA ALTOS platform"
